custom_homepage_enabled:
  type: bool
  default: true
  client: true
  description:
    en: "Enable custom homepage layout."

custom_homepage_blocks:
  type: list
  default: "https://example.com/block1, https://example.com/block2, https://example.com/block3, https://example.com/block4, https://example.com/block5"
  client: true
  description:
    en: "Comma-separated list of URLs for custom homepage blocks."
